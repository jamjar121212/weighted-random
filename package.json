{
  "name": "weighted-random",
  "version": "1.0.0",
  "description": "randomly pick from a weighted distribution.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.0"
  }
}
